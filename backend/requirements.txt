# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9
asyncpg==0.29.0
pymongo==4.6.0

# Async Task Processing
celery==5.3.4
redis==5.0.1
flower==2.0.1

# ==============================================================================
# LLM PROVIDERS
# ==============================================================================

# Google AI - Gemini (default)
google-generativeai==0.3.2
google-cloud-aiplatform==1.38.1

# OpenAI (optional - install if using OpenAI or Azure OpenAI)
openai>=1.0.0

# Anthropic (optional - install if using Claude)
anthropic>=0.18.0

# AWS Bedrock (optional - install if using AWS)
boto3>=1.34.0

# Ollama (uses httpx which is already included)

# ==============================================================================
# STORAGE PROVIDERS
# ==============================================================================

# Google Cloud Storage (default)
google-cloud-storage==2.14.0

# Azure Blob Storage (optional - install if using Azure)
azure-storage-blob>=12.19.0
azure-identity>=1.15.0

# AWS S3 (uses boto3 which is already included above)

# ==============================================================================
# VISION/OCR PROVIDERS
# ==============================================================================

# Google Cloud Vision (optional - install if using Google Vision)
google-cloud-vision>=3.5.0

# Azure Computer Vision (optional - install if using Azure Vision)
azure-cognitiveservices-vision-computervision>=0.9.0
azure-ai-formrecognizer>=3.3.0

# AWS Textract (uses boto3 which is already included above)

# Tesseract (local OCR - default)
pytesseract>=0.3.10
Pillow==10.1.0

# ==============================================================================
# DATA VALIDATION & SECURITY
# ==============================================================================

# Data Validation
pydantic==2.5.2
pydantic-settings==2.1.0
email-validator==2.1.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt>=4.0.0
python-keycloak==3.7.0
python-magic==0.4.27  # MIME type detection for file uploads

# ==============================================================================
# UTILITIES
# ==============================================================================

python-dotenv==1.0.0
httpx==0.25.2
aiofiles==23.2.1
tenacity==8.2.3
pytz==2024.1  # Timezone handling

# PDF Processing
PyPDF2==3.0.1
pdf2image==1.16.3
PyMuPDF==1.24.0

# Data Processing
pandas==2.1.4
numpy==1.26.2

# Embeddings (local)
sentence-transformers>=5.0.0

# Logging & Monitoring
loguru==0.7.2
sentry-sdk==1.39.1

# ==============================================================================
# TESTING & CODE QUALITY
# ==============================================================================

pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

black==23.12.1
flake8==6.1.0
mypy==1.7.1
