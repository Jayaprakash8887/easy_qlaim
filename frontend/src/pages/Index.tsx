// This file is no longer used - Dashboard.tsx is the main dashboard
// Keeping for reference, redirect to Dashboard
import { Navigate } from "react-router-dom";

const Index = () => {
  return <Navigate to="/dashboard" replace />;
};

export default Index;
